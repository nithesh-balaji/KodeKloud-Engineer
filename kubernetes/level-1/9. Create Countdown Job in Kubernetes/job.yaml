apiVersion: batch/v1
kind: Job
metadata:
  name: countdown-nautilus
spec:
  template:
    metadata:
      name: countdown-nautilus
    spec:
      containers:
      - image: fedora:latest
        name: container-countdown-nautilus
        command: ["sleep", "5"]
      restartPolicy: Never